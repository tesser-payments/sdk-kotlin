name: Release

# Runs on every push to main. The `gate` job reads the project version from
# `gradle.properties` and skips the rest of the workflow if a `v<version>` tag
# already exists. To cut a new release, bump `version=` in `gradle.properties`
# and push to main; the workflow publishes to Maven Central and creates the tag.
on:
  push:
    branches: [main]

# `contents: write` is needed so the publish job can push the version tag.
permissions:
  contents: write

jobs:
  gate:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.read.outputs.version }}
      should_release: ${{ steps.check.outputs.should_release }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          fetch-depth: 0   # full history so `git rev-parse` can see existing tags
      - id: read
        name: Read project version from gradle.properties
        run: |
          VERSION=$(grep '^version=' gradle.properties | cut -d= -f2)
          if [ -z "$VERSION" ]; then
            echo "::error::Could not read 'version=' from gradle.properties" >&2
            exit 1
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Project version: $VERSION"
      - id: check
        name: Skip if this version is already released
        run: |
          if git rev-parse "v${{ steps.read.outputs.version }}" >/dev/null 2>&1; then
            echo "Tag v${{ steps.read.outputs.version }} already exists. No release needed."
            echo "should_release=false" >> "$GITHUB_OUTPUT"
          else
            echo "Tag v${{ steps.read.outputs.version }} does not exist. Releasing."
            echo "should_release=true" >> "$GITHUB_OUTPUT"
          fi

  publish:
    needs: gate
    if: needs.gate.outputs.should_release == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
        with:
          fetch-depth: 0
      - uses: actions/setup-java@c1e323688fd81a25caa38c78aa6df2d33d3e20d9 # v4.8.0
        with:
          distribution: temurin
          java-version: 17
      - uses: gradle/actions/setup-gradle@ed408507eac070d1f99cc633dbcf757c94c7933a # v4.4.3

      - name: Verify build
        run: ./gradlew build --no-daemon --stacktrace

      - name: Publish to Maven Central (Sonatype Central Portal)
        env:
          # Vanniktech-recognized env-var names. Configure these in
          # GitHub > Settings > Secrets and variables > Actions:
          ORG_GRADLE_PROJECT_mavenCentralUsername: ${{ secrets.SONATYPE_CENTRAL_USERNAME }}
          ORG_GRADLE_PROJECT_mavenCentralPassword: ${{ secrets.SONATYPE_CENTRAL_PASSWORD }}
          ORG_GRADLE_PROJECT_signingInMemoryKey: ${{ secrets.GPG_PRIVATE_KEY }}
          ORG_GRADLE_PROJECT_signingInMemoryKeyPassword: ${{ secrets.GPG_PASSPHRASE }}
        run: ./gradlew :sdk:publishAndReleaseToMavenCentral --no-daemon --stacktrace

      - name: Tag the release and create a GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          TAG="v${{ needs.gate.outputs.version }}"
          git tag "$TAG"
          git push origin "$TAG"
          gh release create "$TAG" --generate-notes
